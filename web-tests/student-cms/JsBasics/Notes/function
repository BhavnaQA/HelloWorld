Function is not executed by its own, it needs to be called
    Ex function add(){
        //body
    }

    add()

Function expression 
    let calc = function (number){
        //code
        return result;
    }

    calc(x)

Also check arrow function